# Archive

`original-prototype.html` is the original single-file version of the booking
tool, kept for reference only. It is **not** served and is not part of the
running application — it stores bookings in browser memory with no server, no
accounts and no persistence.

The live application is the `public/` + `server/` pair. Nothing here is loaded
by it, and nothing here should be edited.
